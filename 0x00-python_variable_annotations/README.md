Python annotations are a way to add metadata to function arguments and return
values. This metadata can be used by tools to provide additional information.

example of annotations:
```Python annotations```
def foo(a: int, b: str) -> float:)
    pass


